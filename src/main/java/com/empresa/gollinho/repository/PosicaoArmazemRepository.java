package com.empresa.gollinho.repository;

import com.empresa.gollinho.model.PosicaoArmazem;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PosicaoArmazemRepository extends JpaRepository<PosicaoArmazem, Long> {
}
